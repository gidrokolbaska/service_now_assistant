import 'package:flutter/material.dart';

class MyCasesViewWidget extends StatelessWidget {
  const MyCasesViewWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(color: Colors.red),
    );
  }
}
