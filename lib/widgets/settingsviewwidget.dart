import 'package:flutter/material.dart';

class SettingsViewWidget extends StatelessWidget {
  const SettingsViewWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Container(color: Colors.blue),
    );
  }
}
